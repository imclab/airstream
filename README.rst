Airstream: Take your PYTHONHOME with you
========================================

This package replicates the basic functionality of `virtualenv --relocatable`
for any PYTHONHOME.

Nice and simple.

It currently supports:

- Rewriting hashbangs

It will eventuall support:

- Rewriting .pth and .egg-links